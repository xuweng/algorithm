package com.leetcode.tag.tree.two;

/**
 * 1372. 二叉树中的最长交错路径
 * <p>
 * 用一个栈就可以完成遍历,不用cur
 * <p>
 * 用一个栈。不用cur。
 * <p>
 * 虽然简单,就是想不出来
 */
public class LongestZigZag {
    class Solution {
        public int longestZigZag(TreeNode root) {
            return 0;
        }
    }

    class TreeNode {
        int val;
        TreeNode left;
        TreeNode right;

        TreeNode(int x) {
            val = x;
        }
    }
}
